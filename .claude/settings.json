{
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if grep -q 'github-token'; then echo '{\"decision\":\"block\",\"reason\":\"Access to .devcontainer/github-token is blocked to prevent token leakage.\"}'; exit 2; fi",
            "statusMessage": "Checking for token file access..."
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "echo '{\"decision\":\"block\",\"reason\":\"Prohibited operation. Ask the user to stage and commit manually.\"}'; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "echo '{\"decision\":\"block\",\"reason\":\"Prohibited operation. Ask the user to push manually.\"}'; exit 2"
          }
        ]
      }
    ]
  }
}