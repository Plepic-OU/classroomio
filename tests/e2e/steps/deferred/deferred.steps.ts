import { createBdd } from 'playwright-bdd';

const { Given, Then } = createBdd();

Given('this scenario is not yet implemented', async () => {
  // Placeholder for @skip scenarios. Never runs — the @skip tag prevents execution.
});

Then('skip', async () => {
  // Placeholder for @skip scenarios.
});
