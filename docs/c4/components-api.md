# C4 — Layer 3: API Components

> Generated by `/c4-model` skill on 2026-03-13.
> Source: AST extracted from `apps/api` (30 raw components → 9 aggregated groups).
> Refresh: run `/c4-model` in Claude Code.

## Diagram

```mermaid
C4Component
  title Components — API Service (Hono)

  Container_Boundary(api, "API Service — Hono on Node.js") {
    Component(CourseRoutes, "Course Routes", "Hono router", "PDF certificate download, course content download, cloning, KaTeX rendering, presigned URLs — routes/course")
    Component(MailRoutes, "Mail Routes", "Hono router", "Transactional email dispatch (invite, welcome, exercise updates) — routes/mail, services/mail")
    Component(CourseServices, "Course Services", "TypeScript", "Course cloning business logic — services/course")
    Component(CertUtils, "Certificate Utils", "TypeScript", "PDF certificate generation and Cloudflare R2 upload — utils/certificate, utils/cloudflare, utils/s3")
    Component(CoursePDFUtils, "Course PDF Utils", "TypeScript", "Course content PDF generation — utils/course, utils/lesson, utils/upload")
    Component(AuthUtils, "Auth Utils", "TypeScript", "JWT validation middleware and Supabase client — utils/auth, utils/supabase")
    Component(RedisUtils, "Redis Utils", "TypeScript", "Rate limiter with Upstash Redis, key generators — utils/redis")
    Component(Middleware, "Middleware", "Hono middleware", "Rate limiting (per IP/user), auth validation — middlewares/")
    Component(Config, "Config", "Zod + TypeScript", "Env var validation at startup, constants, OpenAPI spec — config/env, constants")
  }

  ContainerDb_Ext(supabase_ext, "Supabase REST/RPC", "PostgREST + GoTrue", "Course data (service role)")
  ContainerDb_Ext(redis_ext, "Redis", "Upstash / Redis", "Rate limit counters")
  System_Ext(cloudflare_r2, "Cloudflare R2", "S3-compatible storage", "Generated PDFs and media")
  System_Ext(email_service, "Email Service", "ZeptoMail / Nodemailer", "Transactional emails")

  Rel(CourseRoutes, Middleware, "Auth-protected endpoints use auth middleware")
  Rel(CourseRoutes, CourseServices, "Delegates course cloning")
  Rel(CourseRoutes, CertUtils, "Generates and uploads certificate PDFs")
  Rel(CourseRoutes, CoursePDFUtils, "Generates course content PDFs")
  Rel(CourseRoutes, Config, "Reads env constants and upload limits")
  Rel(CourseRoutes, Config, "Validated request types via Zod schemas")
  Rel(MailRoutes, Config, "Reads SMTP and env config")
  Rel(MailRoutes, Config, "Typed mail request/response schemas")
  Rel(Middleware, AuthUtils, "Calls JWT validator for protected routes")
  Rel(Middleware, RedisUtils, "Applies rate limit checks")
  Rel(Middleware, Config, "Reads rate limit constants")
  Rel(CourseServices, AuthUtils, "Uses Supabase client for DB access")
  Rel(CertUtils, Config, "Reads Cloudflare R2 credentials")
  Rel(CoursePDFUtils, CertUtils, "Uses Cloudflare upload helper")
  Rel(RedisUtils, AuthUtils, "Looks up user via Supabase for per-user limits")
  Rel(RedisUtils, Config, "Reads Redis URL and rate limit constants")
  Rel(AuthUtils, Config, "Reads Supabase service role key and URL")

  Rel(CourseServices, supabase_ext, "Queries and mutates course data", "HTTPS REST (service role)")
  Rel(Middleware, redis_ext, "Checks and increments rate limit counters", "Redis protocol")
  Rel(CertUtils, cloudflare_r2, "Uploads generated certificate PDFs", "S3 SDK / HTTPS")
  Rel(CoursePDFUtils, cloudflare_r2, "Uploads generated course PDFs", "S3 SDK / HTTPS")
  Rel(MailRoutes, email_service, "Dispatches emails via SMTP or API", "SMTP / HTTPS")

  UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

## Notes

- `utils/openapi` is folded into Config (generates OpenAPI spec from env metadata)
- `utils/genUniqueId.ts`, `utils/email.ts`, `utils/mail.ts` folded into MailRoutes/CertUtils as leaf utilities
- `services/mail.ts` folded into MailRoutes group (mail service used only by the mail router)
- Root files `app.ts`, `index.ts`, `rpc-types.ts` are app bootstrap and type export — omitted from component diagram
- Middleware stack order: Logger → Pretty JSON → Secure Headers → CORS → Rate Limiter
