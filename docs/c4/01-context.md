# ClassroomIO — Layer 1: System Context

<!-- generated by .claude/skills/c4-model from docs/c4/.cache/*.json -->

ClassroomIO is a learning-management platform. The same SvelteKit app serves
both the teacher / org dashboard and the student LMS; a separate Hono service
handles long-running media and email work. External integrations are limited
to a few well-defined boundaries: Supabase (database, auth, storage), OpenAI
(course-content generation), PostHog (analytics), Polar (payments), Resend /
SMTP (email), and S3-compatible object storage (Cloudflare R2). Redis backs
rate-limiting inside the API.

```mermaid
C4Context
  title ClassroomIO — System Context (Layer 1)

  Person(student, "Student", "Takes courses, quizzes, exercises")
  Person(instructor, "Instructor", "Authors courses, runs cohorts")
  Person(orgadmin, "Org admin", "Manages an organisation's instance")

  System(classroomio, "ClassroomIO", "Open-source LMS — dashboard + LMS + media/email service")

  System_Ext(openai, "OpenAI", "LLM for course/lesson generation")
  SystemDb_Ext(supabase, "Supabase", "Postgres + Auth + Storage")
  System_Ext(posthog, "PostHog", "Product analytics")
  System_Ext(polar, "Polar", "Subscriptions / billing")
  System_Ext(mailprov, "Email provider", "Resend / SMTP")
  SystemDb_Ext(s3, "Cloudflare R2", "S3-compatible object storage")
  SystemDb_Ext(redis, "Redis", "Rate-limit / cache")

  Rel(student, classroomio, "Learns in")
  Rel(instructor, classroomio, "Authors and runs courses in")
  Rel(orgadmin, classroomio, "Configures the org instance via")

  Rel(classroomio, supabase, "Reads/writes auth, course and org data")
  Rel(classroomio, openai, "Generates lesson content")
  Rel(classroomio, posthog, "Sends product events")
  Rel(classroomio, polar, "Manages subscriptions")
  Rel(classroomio, mailprov, "Sends transactional email")
  Rel(classroomio, s3, "Stores uploads, generated PDFs, video assets")
  Rel(classroomio, redis, "Rate-limits and caches")
```
