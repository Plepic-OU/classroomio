# ClassroomIO — Layer 2: Containers

<!-- generated by .claude/skills/c4-model from docs/c4/.cache/*.json -->

ClassroomIO ships as two long-running processes plus a managed Postgres. The
**Dashboard** is a single SvelteKit application that hosts both the teacher /
org UI (`/courses`, `/org`, `/home`, ...) and the student LMS (`/lms/*`) — they
share routing, layouts, and `src/lib`. Its `src/routes/api/*` endpoints run
inside the SvelteKit server. The **API** is a Hono service on Node that
handles work that doesn't belong in a request lifecycle: PDF certificates,
video processing, transactional email, S3 / Cloudflare R2 uploads, Redis-backed
rate limits, and the typed-RPC types the dashboard consumes. **Supabase** is
the single source of truth for auth, Postgres data, and user-uploaded files.
The flag `PUBLIC_IS_SELFHOSTED` switches the dashboard between the Vercel and
Node adapters; everything else is environment-agnostic.

```mermaid
C4Container
  title ClassroomIO — Containers (Layer 2)

  Person(user, "User", "Student, instructor, or org admin")

  Container_Boundary(cio, "ClassroomIO") {
    Container(dashboard, "Dashboard", "SvelteKit (Node or Vercel adapter)", "Teacher dashboard + student LMS in one app. Routes at /courses, /org, /lms, /home and internal API at /api/*.")
    Container(api, "API", "Hono on @hono/node-server (port 3002)", "PDF / video / email pipelines, S3 uploads, Redis-backed rate limits. Exposes typed RPC consumed by the dashboard as @cio/api.")
    ContainerDb(supabase, "Supabase", "Postgres + Auth + Storage", "Schema source of truth; migrations under supabase/migrations.")
  }

  System_Ext(openai, "OpenAI", "Course / lesson content generation")
  System_Ext(posthog, "PostHog", "Product analytics")
  System_Ext(polar, "Polar", "Subscriptions / billing")
  System_Ext(mailprov, "Resend / SMTP", "Transactional email")
  SystemDb_Ext(s3, "Cloudflare R2", "S3-compatible object storage")
  SystemDb_Ext(redis, "Redis", "Rate-limit / cache")

  Rel(user, dashboard, "Uses", "HTTPS")
  Rel(dashboard, api, "Long-running jobs + RPC types", "HTTPS / typed RPC")
  Rel(dashboard, supabase, "Reads/writes course, org, user data", "Postgres + REST (Supabase SDK)")
  Rel(dashboard, openai, "Streams lesson generation", "HTTPS (Vercel AI SDK)")
  Rel(dashboard, posthog, "Sends product events", "HTTPS")
  Rel(dashboard, polar, "Subscription webhooks + checkout", "HTTPS")

  Rel(api, supabase, "Auth + data", "Postgres + REST")
  Rel(api, s3, "Presigns + uploads media", "S3 API")
  Rel(api, redis, "Rate-limit, cache", "Redis protocol")
  Rel(api, mailprov, "Sends mail", "SMTP / HTTPS")
```
