# C4 — Layer 3: Dashboard Components

> Generated by `/c4-model` skill on 2026-03-13.
> Source: AST extracted from `apps/dashboard` (96 raw components → 11 aggregated groups).
> Refresh: run `/c4-model` in Claude Code.

## Diagram

```mermaid
C4Component
  title Components — Dashboard (SvelteKit)

  Container_Boundary(dashboard, "Dashboard — SvelteKit 1.x") {
    Component(ServerHooks, "Server Hooks", "SvelteKit hooks.server.ts", "Validates JWT on all /api/* routes; injects user ID into request headers")
    Component(OrgAdminRoutes, "Org Admin Routes", "SvelteKit routes", "Course management, audience, quiz, settings, org setup — routes/org, routes/courses")
    Component(LMSRoutes, "LMS Routes", "SvelteKit routes", "Student portal — home, explore, my learning, community, exercises — routes/lms")
    Component(CourseRoutes, "Course Routes", "SvelteKit routes", "Public course landing pages — routes/course")
    Component(ServerAPIRoutes, "Server API Routes", "SvelteKit server routes", "Server-side handlers for email, analytics, org, courses, polar, admin — routes/api")
    Component(AuthRoutes, "Auth Routes", "SvelteKit routes", "Login, signup, onboarding, invite flows — routes/login, signup, invite")
    Component(UIComponents, "UI Components", "Svelte 4 components", "Feature UI: Course, Org, LMS, Navigation, Forms, Editors, Modals — lib/components")
    Component(Services, "Services", "TypeScript", "Data-fetching via Supabase RPC and Hono API client — lib/utils/services")
    Component(Stores, "Stores", "Svelte writable stores", "Global state: app theme, user profile, org, attendance — lib/utils/store")
    Component(Utilities, "Utilities", "TypeScript", "Pure helpers, Supabase browser/server clients, constants, i18n — lib/utils/functions, constants")
    Component(Types, "Types", "TypeScript", "Shared TypeScript type definitions — lib/utils/types")
  }

  Container_Ext(supabase_api, "Supabase REST/RPC", "PostgREST + GoTrue", "Data queries and auth")
  Container_Ext(api_service, "API Service", "Hono on Node.js", "PDF, certificates, clone, presign")

  Rel(ServerHooks, Services, "Validates auth token, reads user session")
  Rel(OrgAdminRoutes, Services, "Fetches org/course/audience data")
  Rel(OrgAdminRoutes, Stores, "Reads org context")
  Rel(OrgAdminRoutes, Utilities, "Calls Supabase server client, helper fns")
  Rel(CourseRoutes, Services, "Loads course details for landing page")
  Rel(CourseRoutes, Utilities, "Calls Supabase browser client")
  Rel(ServerAPIRoutes, Services, "Fetches data with service-role client")
  Rel(ServerAPIRoutes, Utilities, "Calls Supabase server client")
  Rel(AuthRoutes, Services, "Validates invites, loads org context")
  Rel(AuthRoutes, Utilities, "Calls Supabase browser client")
  Rel(UIComponents, Services, "Fetches and mutates course/org data")
  Rel(UIComponents, Utilities, "Uses pure helpers and Supabase client")
  Rel(UIComponents, Types, "Typed props and state")
  Rel(Services, Stores, "Updates global app/org/user state")
  Rel(Services, Utilities, "Uses Supabase client and helpers")
  Rel(Services, Types, "Typed request and response shapes")
  Rel(Utilities, Stores, "Reads and updates app state")
  Rel(Utilities, Types, "Typed return values")
  Rel(Stores, Types, "Typed store shapes")

  Rel(Services, supabase_api, "Supabase RPC/REST — data queries", "HTTPS")
  Rel(Services, api_service, "Hono RPC — PDF, certificates, clone", "HTTP hcWithType")
  Rel(ServerHooks, supabase_api, "Validates access token (GoTrue)", "HTTPS")

  UpdateLayoutConfig($c4ShapeInRow="4", $c4BoundaryInRow="1")
```

## Notes

- `lib/components/Course` has 83 files (15 .ts + 68 .svelte) — largest component group; consider reviewing if depth-3 extraction should be increased for more granular Layer 3 breakdown
- `mail/sendEmail.ts` (dashboard-side email util) is folded into Services group
- Root layout files (`routes/+layout.server.ts`, `routes/+layout.ts`) folded into OrgAdminRoutes for diagram clarity
- Circular dependencies exist between UIComponents ↔ Utilities ↔ Services; this is typical for SvelteKit apps with shared helpers
- `ServerAPIRoutes` uses `getServerSupabase()` (service-role client) — these routes bypass RLS
