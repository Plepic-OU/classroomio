# Layer 3 — API components

> Generated by `.claude/skills/c4-model` from `docs/c4/.extraction/api.json`.
> Source root: `apps/api/src`, depth=2.

```mermaid
C4Component
  title API — components (depth=2)

  Container_Boundary(boundary, "API") {
    Component(root, "(root)", "3 ts", "3 files")
    Component(config, "config", "1 ts", "1 files")
    Component(constants, "constants", "3 ts", "3 files")
    Component(middlewares, "middlewares", "2 ts", "2 files")
    Component(routes, "routes", "1 ts", "1 files")
    Component(routes_course, "routes/course", "5 ts", "5 files")
    Component(services, "services", "1 ts", "1 files")
    Component(services_course, "services/course", "1 ts", "1 files")
    Component(types, "types", "3 ts", "3 files")
    Component(types_course, "types/course", "2 ts", "2 files")
    Component(utils, "utils", "10 ts", "10 files")
    Component(utils_auth, "utils/auth", "1 ts", "1 files")
    Component(utils_openapi, "utils/openapi", "1 ts", "1 files")
    Component(utils_redis, "utils/redis", "3 ts", "3 files")
  }

  Rel(routes_course, utils, "uses ×6")
  Rel(routes_course, types_course, "uses ×4")
  Rel(utils, types_course, "uses ×3")
  Rel(middlewares, utils_redis, "uses ×2")
  Rel(routes_course, constants, "uses ×2")
  Rel(routes_course, middlewares, "uses ×2")
  Rel(services, utils, "uses ×2")
  Rel(utils, config, "uses ×2")
  Rel(utils, constants, "uses ×2")
  Rel(root, config, "uses")
  Rel(root, middlewares, "uses")
  Rel(root, routes, "uses")
  Rel(root, routes_course, "uses")
  Rel(root, utils_openapi, "uses")
  Rel(constants, config, "uses")
  Rel(middlewares, constants, "uses")
  Rel(middlewares, utils_auth, "uses")
  Rel(routes, config, "uses")
  Rel(routes, services, "uses")
  Rel(routes, types, "uses")
  Rel(routes_course, services_course, "uses")
  Rel(routes_course, types, "uses")
  Rel(services, config, "uses")
  Rel(services, types, "uses")
  Rel(services_course, types, "uses")
  Rel(services_course, utils, "uses")
  Rel(types, types_course, "uses")
  Rel(types_course, constants, "uses")
  Rel(utils_auth, utils, "uses")
  Rel(utils_openapi, config, "uses")
  Rel(utils_redis, config, "uses")
  Rel(utils_redis, constants, "uses")
  Rel(utils_redis, utils, "uses")
```

- Components kept: **14** of 14
- Edges drawn: **33** of 33
