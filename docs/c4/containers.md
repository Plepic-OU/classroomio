# C4 — Layer 2: Containers

> Generated by `/c4-model` skill on 2026-03-13.
> Source: AST extracted from `apps/dashboard` and `apps/api`.
> Refresh: run `/c4-model` in Claude Code.

## Diagram

```mermaid
C4Container
  title Containers — ClassroomIO LMS

  Person(instructor, "Instructor", "Creates and manages courses")
  Person(student, "Student", "Takes courses and submits exercises")

  Enterprise_Boundary(classroomio_sys, "ClassroomIO System") {
    Container(dashboard, "Dashboard", "SvelteKit 1.x + TailwindCSS", "LMS frontend for org admins and students — port 5173")
    Container(api, "API Service", "Hono on Node.js (ESM)", "PDF generation, email dispatch, file ops, course cloning — port 3002")
    Container(edge_functions, "Edge Functions", "Deno (Supabase)", "Serverless functions for background tasks — supabase/functions/")
    Container(supabase_api, "Supabase REST/RPC", "PostgREST + GoTrue", "REST API, RPC calls, and JWT auth — port 54321")
    ContainerDb(postgres, "PostgreSQL", "PostgreSQL 15 via Supabase", "Primary data store — 39 tables, port 54322")
    ContainerDb(redis, "Redis", "Upstash / Redis", "Rate limiting and caching")
    Container(supabase_storage, "Supabase Storage", "Supabase Storage", "Media blob storage wrapping Cloudflare R2 / local disk")
  }

  System_Ext(zepto_mail, "Email Service", "ZeptoMail / Nodemailer — transactional email")
  System_Ext(cloudflare_r2, "Cloudflare R2", "S3-compatible object storage for PDFs and media")
  System_Ext(polar, "Polar.sh", "Payment and subscription management")
  System_Ext(posthog, "PostHog", "Product analytics")
  System_Ext(sentry, "Sentry", "Error monitoring")

  Rel(instructor, dashboard, "Manages courses, audience, settings", "HTTPS")
  Rel(student, dashboard, "Takes courses, submits exercises", "HTTPS")

  Rel(dashboard, api, "Course PDF, certificates, clone, presign", "HTTP RPC via hcWithType")
  Rel(dashboard, supabase_api, "Data queries and mutations", "Supabase RPC/REST, JS client")
  Rel(dashboard, supabase_api, "User authentication", "Supabase Auth / JWT")
  Rel(dashboard, supabase_storage, "Upload and fetch media files", "HTTPS")
  Rel(dashboard, posthog, "Track usage events", "HTTPS")
  Rel(dashboard, sentry, "Report errors and traces", "HTTPS")
  Rel(dashboard, polar, "Payment and subscription flows", "HTTPS")

  Rel(api, supabase_api, "Course data queries (service role)", "HTTPS REST")
  Rel(api, redis, "Rate limiting per user/IP", "Redis protocol")
  Rel(api, cloudflare_r2, "Upload generated PDFs and media", "S3 SDK / HTTPS")
  Rel(api, zepto_mail, "Dispatch transactional emails", "SMTP / HTTPS")

  Rel(supabase_api, postgres, "Read and write course data", "SQL")
  Rel(edge_functions, postgres, "Background data processing", "SQL")

  UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

## Notes

- Dashboard uses three data-access patterns: direct Supabase RPC (browser client), SvelteKit server API routes (service role), and Hono API client (`ApiClient` with `hcWithType` for type-safe RPC)
- `hooks.server.ts` validates all `/api/*` requests using Supabase access token before they reach SvelteKit server routes
- Build dependency: `dashboard` build depends on `api` build (RPC types exported via `@cio/api/rpc-types`)
- Supabase Storage wraps either Cloudflare R2 (production) or local disk (development) transparently
