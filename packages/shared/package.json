{
  "name": "shared",
  "version": "1.0.0",
  "dependencies": {
    "tsconfig": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  }
}
