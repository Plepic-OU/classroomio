{
  "name": "@cio/e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "bddgen && playwright test",
    "test:e2e": "bddgen && playwright test",
    "test:e2e:ui": "bddgen && playwright test --ui --ui-port=9323 --ui-host=0.0.0.0",
    "report": "playwright show-report playwright-report --host 0.0.0.0 --port 9323",
    "codegen": "playwright codegen http://localhost:5173"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright-bdd": "^8.2.0"
  }
}
